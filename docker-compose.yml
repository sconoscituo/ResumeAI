services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DATABASE_URL=sqlite+aiosqlite:///./resumeai.db
      - DEBUG=False
    volumes:
      - ./resumeai.db:/app/resumeai.db
      - ./pdfs:/app/pdfs
    restart: unless-stopped
